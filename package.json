{
    "name": "aytada-creative-actor",
    "version": "1.0.0",
    "type": "module",
    "main": "dist/main.js",
    "dependencies": {
        "apify": "^3.1.0",
        "crawlee": "^3.0.0",
        "node-fetch": "^3.3.0"
    },
    "devDependencies": {
        "@apify/tsconfig": "^0.1.0",
        "typescript": "^5.0.0"
    },
    "scripts": {
        "start": "npm run build && node dist/main.js",
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
